{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ],
    "framework": "vite",
    "installCommand": "npm install",
    "buildCommand": "npm test && vite build",
    "cleanUrls": true
}